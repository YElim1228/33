<!DOCTYPE HTML>
<html>
<head>

</head>
<body>
    <canvas id="canvas" width="578" height="200"></canvas>
    <script>
        const canvas = document.getElementById('canvas'); // 'cnavas'가 아니라 'canvas'입니다.
        const context = canvas.getContext('2d');

        context.beginPath();
        context.moveTo(100, 150);
        context.lineTo(450, 50);
        context.stroke(); // 라인을 실제로 그리기 위해 stroke() 메서드를 호출합니다.
    </script>
</body>
</html>